{
  "name": "kontur",
  "version": "0.0.0",
  "private": true,
  "description": "A visual-first programming language: maps of AI-authored software. IR core.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "tsx src/cli/validate.ts",
    "transpile": "tsx src/cli/transpile.ts",
    "lift": "tsx src/cli/lift.ts"
  },
  "dependencies": {
    "typescript": "^5.6.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
